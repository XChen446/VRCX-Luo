name: CI
on:
  # pull_request:
  #   branches:
  #     - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint_js:
    name: Lint JS (Oxlint)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: oxc-project/oxlint-action@v2.0.1
        with:
          config: .oxlintrc.json
        continue-on-error: true

  format_js:
    name: Format JS (Oxfmt)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: npm
      - run: npm ci
      - run: npm run format:check

  format_c_sharp:
    name: Format C# (dotnet format)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Setup .NET 9
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '9.0.x'
      - name: Run Dotnet format
        run: |
          dotnet format whitespace --no-restore --verify-no-changes
          dotnet format style --no-restore --verify-no-changes

  # Aggregate gate: all three quality checks must complete before DB tests run.
  # Uses `if: always()` because format_js (oxfmt) is currently red at repo baseline
  # (~1140 files with format drift). Without always(), the gate would never pass,
  # silently blocking MySQL/PgSQL integration tests — the high-value jobs.
  # The test_mysql/test_pgsql jobs ALSO declare `if: always()` because GitHub's
  # default `if: success()` would skip them when any upstream job in the chain
  # failed (e.g. format_js) — even though the immediate need (quality-gate)
  # concluded success. Once format_js baseline drift is fixed, these `if: always()`
  # on the test jobs can be relaxed back to default success() gating if desired.
  quality-gate:
    name: Quality Gate
    runs-on: ubuntu-latest
    needs: [lint_js, format_js, format_c_sharp]
    if: always()
    steps:
      - run: echo "All quality checks complete"

  test_mysql:
    name: Test MySQL (${{ matrix.mysql-version }})
    runs-on: ubuntu-latest
    needs: quality-gate
    if: always()
    strategy:
      fail-fast: false
      matrix:
        mysql-version: ['8.0', '8.4']
    env:
      MYSQL_TEST_HOST: 127.0.0.1
      MYSQL_TEST_PORT: 3306
      MYSQL_TEST_USER: root
      MYSQL_TEST_PASSWORD: root
      MYSQL_TEST_DATABASE: vrcx_test
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Setup MySQL ${{ matrix.mysql-version }}
        uses: shogo82148/actions-setup-mysql@076e636c45996ec8cb1a455c65f9f5364aeebd8a # v1.52.1
        with:
          mysql-version: ${{ matrix.mysql-version }}
          root-password: root
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: npm
      - run: npm ci
      - name: Create test database
        run: mysql -h 127.0.0.1 -u root -proot -e "CREATE DATABASE IF NOT EXISTS vrcx_test;"
      - run: npm test -- --run

  # Phase 9 slice S12 (task 9.15): PostgreSQL 16/17 integration test matrix.
  # Uses ikalnytskyi/action-setup-postgres to provision PG on the runner
  # (supports Linux/macOS/Windows, no Docker service container needed).
  # Tests self-skip when PG_TEST_HOST is unset, so this job is the only
  # place `*.pgsql.test.js` actually executes.
  # Ref: https://github.com/marketplace/actions/setup-postgresql-for-linux-macos-windows
  test_pgsql:
    name: PgSQL Integration (PG ${{ matrix.pg.version }})
    runs-on: ubuntu-latest
    needs: quality-gate
    if: always()
    strategy:
      fail-fast: false
      matrix:
        pg:
          - { version: "16" }
          - { version: "17" }
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      # Pin to v8 tag SHA (c4dda34, 2025-10-10) for supply-chain safety.
      # Bump by updating SHA + tag comment after reviewing upstream release notes.
      - name: Setup PostgreSQL
        uses: ikalnytskyi/action-setup-postgres@c4dda34aae1c821e3a771b68b73b13af3198a7ee # v8
        id: postgres
        with:
          postgres-version: ${{ matrix.pg.version }}
          username: vrcx
          password: vrcx
          database: vrcx
          port: 5432
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Run PgSQL integration tests
        run: npm test -- --run '.pgsql.test.js'
        env:
          PG_TEST_HOST: localhost
          PG_TEST_PORT: 5432
          PG_TEST_USER: vrcx
          PG_TEST_PASSWORD: vrcx
          PG_TEST_DB: vrcx
          PG_TEST_VERSION: ${{ matrix.pg.version }}
          # PGSERVICE enables psql/libpq clients to connect without explicit URI.
          PGSERVICE: ${{ steps.postgres.outputs.service-name }}
